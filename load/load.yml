---
services:
  load:
    image: alpine:3.23
    command:
      - sh
      - -c
      - |
        while :
        do
          :
        done
    deploy:
      resources:
        limits:
          cpus: "0.30"
          memory: "50M"
